export type Database = {
  public: {
    Tables: {
      // Add your table types here after generating them
    }
    Views: {
      // Add your view types here after generating them
    }
    Functions: {
      // Add your function types here after generating them
    }
  }
} 